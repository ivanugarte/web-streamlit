import streamlit as st
st.write("aqui a ver si pasa")

# import plotly.express as px 
# import pandas as pd

# def main():
#     df = pd.read_csv("datos_ventas.csv")
#     df_count = df.groupby("Producto").count().reset_index()
#     st.dataframe(df)  
#     fig = px.pie(df_count, values="Precio_Unitario", names="Region", title="total")
#     st.plotly_chart(fig)

# main()

